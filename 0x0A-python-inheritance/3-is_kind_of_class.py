#!/usr/bin/python3

"""Checks for instance of an object."""

def is_kind_of_class(obj, a_class):
    """return True if obj is an instance, and False if it is not."""
    return isinstance(obj, a_class)
